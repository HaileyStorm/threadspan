---
description: Inspect Threadspan Preview status and choose a bounded cross-provider mode without replacing Claude Code's native picker.
---

Call `bridge_status`. Report only actionable availability and clearly label this Claude Code integration **Preview / live-untested**. Use `consult` for advisory work. Use `delegate` only after the user approves an explicit workspace, allowed and denied paths, non-goals, and finite acceptance commands. Do not claim that Threadspan can replace Claude Code's native model picker or recover a Claude transcript on another host.

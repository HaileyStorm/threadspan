# Notices and prior art

This package is an independent implementation. It does not copy source from the projects below.
They informed protocol choices, compatibility expectations, or testing priorities:

- OpenAI Codex (`openai/codex`), Apache-2.0.
- Cursor SDK documentation and Cursor's public plugin skill repository (`cursor/plugins`).
- `pi-cursor-sdk` (`fitchmultz/pi-cursor-sdk`), MIT, as a reference for preserving Cursor's own agent loop and bridging external tools through MCP.
- Hermes Agent (`NousResearch/hermes-agent`), MIT, including its Nous Portal subscription proxy pattern.
- Codex++ and related desktop-integration experiments, treated as research references only.

No third-party credentials, session cookies, binaries, model weights, or proprietary prompts are bundled.
